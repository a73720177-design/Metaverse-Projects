# EXE 실행 오류·보안 점검 결과

대상: `C:\Users\임준혁\Downloads\최종\EXE폴더\Metaverse-Setup.exe`

이 폴더는 Git 저장소가 아니며, 수정 대상은 EXE에 포함되는 `build\payload`와 빌드 스크립트다. 기존 소스 기준 커밋 정보는 보존하고 manifest에 로컬 수정 식별자 `exe-audit-20260920`을 추가했다.

## 수정한 항목

| 항목 | 확인한 문제 | 수정 |
| --- | --- | --- |
| 로컬 웹서버 Host 검증 | `Host: audit.invalid`로 로컬 health API가 HTTP 200 응답. DNS rebinding 방어가 없었음 | localhost·127.0.0.1만 허용하고 나머지는 403. 전달 IP도 클라이언트 제공값 대신 실제 연결 주소로 설정 |
| 서버 종료 | `--stop`이 종료 전에 모델 기본값·환경설정을 다시 저장하여 9B 설정이 4B로 바뀔 수 있음 | 해당 설치의 Docker 프로젝트 라벨로 컨테이너를 찾아 설정 저장 전에 종료. GPU 설정과 무관하게 해당 프로젝트의 vLLM도 포함 |
| 설치 완료 검사 | 실제 생성 공급자가 vLLM이어도 Ollama 생성만 검사하여 vLLM 생성 실패를 놓치고 불필요한 모델 로딩을 유발 | 선택된 공급자의 실제 생성 API 검사. vLLM 사용 시 vLLM 생성 + Ollama 임베딩 검사 |
| 서비스 준비 상태 | Ollama 모델의 태그를 제거해 다른 크기 모델도 정상으로 판정. vLLM 모드는 임베딩 모델 확인을 생략 | 정확한 모델 태그·vLLM 제공 모델 확인, 두 공급자 모두 임베딩 모델 확인 |
| 특수문자 설치 경로 | Docker 인수를 cmd.exe로 재해석하여 `%PATH%` 같은 문자열이 환경변수로 확장될 수 있음 | Docker 실행 파일에 인수를 직접 전달. 오류 출력과 실패 종료 코드 보존 |
| 실패 진단 로그 | vLLM을 사용하는데 실패 시 로그 수집 대상에서 vLLM 누락 | 해당 Compose 프로젝트의 모든 서비스 로그 수집 |

관련 소스 위치:

- `build\payload\setup.ps1`: 종료 처리, Docker 호출, 진단 로그
- `build\payload\web\nginx.conf`: Host 검증 및 전달 IP
- `build\payload\llm-service\app\installer_check.py`: 공급자별 실제 생성 검사
- `build\payload\llm-service\app\llm_client.py`: 준비 상태 판정
- `build\build.ps1`: 수정 식별자 포함 재패키징

Host 검증 방식 참고: https://nginx.org/en/docs/http/request_processing.html

## 검증 결과

- 기존 설치 시나리오 12개 통과: WSL, 재시작 분류, 다운로드 재시도, 모델 선택. 모의 검사이며 실제 Windows 기능 설치를 수행하지 않음.
- 추가 PowerShell 회귀 검사 3개 통과: 한글·공백·퍼센트·셸 문자 인수 보존, 오류 출력·종료 코드, 프로젝트 한정 종료. 실제 사용자 서버 종료는 수행하지 않음.
- AI 설치 검사 모의 테스트 6개 통과.
- 모델 준비 상태 테스트 5개 통과: 잘못된 모델 태그, latest 별칭, 임베딩 누락, vLLM 별칭 불일치, 비정상 응답.
- Nginx 설정 검사 통과.
- 새 EXE 직접 `--check` 실행: 압축 해제와 127개 파일 무결성 검사 통과.
- 현재 로컬 앱에 웹·LLM 이미지 수정분 적용. DB·업로드·모델 볼륨 삭제 없음.
- 실제 `vLLM qwen3.5:9b` 응답 생성과 `bge-m3` 임베딩 검사 통과.
- 정상 Host localhost·127.0.0.1: HTTP 200. 잘못된 Host: 수정 전 200 → 수정 후 403.
- 홈페이지·통합 상태 API: HTTP 200. 미인증 agents·documents·chats API: HTTP 401.
- 개발자 상태 API: 전체 ok, 공급자 vLLM, GPU operational=true, 병렬 작업 수 2. GPU 값은 앱 진단 응답이며 별도의 사용률 측정값은 아님.
- EXE SHA256과 `SHA256.txt` 일치.

SHA256: `D567DD9B423920A168EABF1B52E6E8C0980CDA9DDE8DE2FB4FCA1EAAF5D1E71E`

## 사용 및 복구 위치

수정본 실행: 이 폴더의 `Metaverse-Setup.exe`를 더블클릭. 현재 앱에는 웹·LLM 수정분이 이미 반영되어 있고, 로컬 설치 위치의 실행 파일도 새 EXE로 갱신됨.

백업: `C:\Users\임준혁\Downloads\최종\EXE폴더\backup-before-exe-audit-20260920-212629`

백업에는 변경 전 EXE, SHA256, build 폴더가 있음. 사용자 계정·DB·문서의 별도 백업 파일을 생성한 것은 아님.

## 검증 한계

새 PC에서 Docker/WSL 최초 설치·재부팅, 모든 GPU 기종, 모든 문서 형식의 파서 취약점, 전체 의존성 CVE 및 장시간 부하 검사는 이번 범위에서 검증하지 않음. 실제 회원가입부터 문서 업로드·예상 질문·스트리밍까지 전체 시나리오를 다시 수행하지는 않았음. 이번 결과가 모든 오류·취약점의 부재를 의미하지는 않음.

4B 선택 시 Qwen3.5 양자화 모델을 `qwen3:4b` 별칭으로 제공하는 기존 구성은 유지됨. 화면의 이름과 실제 모델 계열을 일치시키는 변경은 수행하지 않음.
