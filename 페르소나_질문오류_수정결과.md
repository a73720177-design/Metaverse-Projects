# 페르소나·예상 질문 오류 수정 결과

## 확인한 원인

1. 세 페르소나의 description에는 이름·직함만 있었고 expertise/evaluation_style은 모두 빈 배열로 저장되어 있었음. 연결 참고자료는 실제로 읽혔으며 각각 2,368~2,980자가 생성 요청에 포함되어 있었음. 따라서 파일 미첨부나 PDF 읽기 실패로 볼 근거는 없음.
2. 운영·예산 질문을 재현하자 첫 요청은 HTTP 200, 보충 요청은 HTTP 422. 보충 시 후보 수가 6개에서 9개로 늘었고 이전 질문까지 포함해 4,096토큰 모델 컨텍스트 한도를 초과함. 기존 화면은 이를 모두 '모델 응답을 받지 못했습니다'로 표시했음.
3. 실제 검증 과정에서 페르소나 입력 길이 초과와 간헐적인 JSON 응답 해석 실패도 확인됨. 출력 예산과 요청 크기를 함께 조절하고 구조화 응답 재시도를 추가함.

## 수정

- 질문 후보를 최대 3개씩, 최대 6회 범위에서 보충. 검증·중복 제거는 계속 적용.
- 제외 질문·페르소나 설명·추가 지침·참고자료를 합산해 입력 예산 조절. 언어 재시도 문구에 필요한 공간도 확보.
- 질문 문장은 120자, 평가 초점은 40자 이내로 요청하고 출력 예산 확대. 근거 ID가 질문 본문에 나타나지 않도록 지침 추가.
- JSON 형식 오류 1회 재시도, 묶음 생성 실패 후에도 남은 한도 내 보충. 연결 실패와 요청 형식·길이 오류를 구분해 안내.
- 페르소나를 실제 인물의 자격 인증이 아닌 발표 연습용 역할로 생성. 연결 자료의 평가 기준과 원문 인용을 명확히 요청.
- 모델이 평가 기준을 비워도 사용자 입력 설명을 user_stated 관점으로 보존. 이를 실제 전문 자격으로 승격하지 않음.
- 참고자료 연결 변경 시 페르소나의 평가 기준도 다시 계산. 생성 실패 시 기존 저장 상태 유지.
- 화면의 세 페르소나는 백업 후 연결 자료로 재생성 완료. 기존 질문 기록은 덮어쓰지 않고 새 연습 결과를 저장.

## 최종 실검사

- 이지은 학생경험 연구자: 6/6개
- 박서준 기술 아키텍트: 6/6개
- 김민정 시설운영 책임자: 6/6개
- 새 세션: `034469df-bde4-4e24-9282-b5a6f45fc13c`
- 최종 세션에 모델 응답 실패 및 '전문 분야·평가 스타일 없음' 경고 없음.
- 기술·운영 페르소나에는 검증되지 않은 일부 특성을 제외했다는 안내가 남아 있음. 전체 원문 중 일부 발췌를 사용했다는 안내도 유지. 둘 다 정상적인 분석 한계 안내이며 경고를 숨기는 방식으로 제거하지 않음.
- 회귀 검사 7개 통과, 새 EXE 직접 실행 무결성 검사 127개 파일 통과, 현재 앱 통합 상태 ok.
- 실제 브라우저 화면 조작 검증은 수행하지 않았으며, 서비스 호출 및 저장된 결과로 검증.

## 위치와 사용

- 실행 파일: `C:\Users\임준혁\Downloads\최종\EXE폴더\Metaverse-Setup.exe`
- 현재 앱의 Backend·LLM과 로컬 설치 EXE도 갱신 완료.
- 기존 화면은 이전 결과를 표시할 수 있으므로 새로고침 후 질문을 다시 생성하면 수정된 흐름이 적용됨.
- 수정 전 코드·EXE 백업: `backup-before-practice-fix-20260920-213905`
- 수정 전 세 페르소나 설정: `build\check-output\demo-personas-before-repair.json`
- 실검사 결과: `build\check-output\demo-practice-repair-result.json`
- 변경 소스: `build\payload\backend\app\services\persona_service.py`, `practice_service.py`, `build\payload\backend\app\integrations\llm\generators.py`, `build\payload\backend\app\controllers\agent_controller.py`, `build\payload\llm-service\app\main.py`, `prompts.py`

모델 출력은 매번 달라질 수 있으며, 자료의 근거나 서로 다른 질문이 부족하면 요청 수보다 적게 제공하는 검증 정책은 유지됨.
