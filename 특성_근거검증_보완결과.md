# 특성 근거 검증 보완

대상: `build\payload\backend\app\services\persona_service.py`의 `_verify_traits`부터 저장소·화면 경고·LLM 프롬프트까지 확인.

## 확인 및 수정

| 항목 | 기존 문제 | 변경 |
| --- | --- | --- |
| 인용 형식 | 공백 묶음만 정리하여 PDF 단어 내부 줄바꿈이나 전각 문자 차이로 정상 인용 탈락 가능 | NFKC 및 공백 정규화. 문장부호·부정 표현·순서는 제거하지 않음 |
| 짧은 근거 | 한 글자도 원문에 포함되면 통과 | 기본적으로 영숫자·한글 등 유효 문자 4개 이상 요구. 2~3자는 특성 값 전체와 같을 때만 허용 |
| 근거 출처 | 출처 검사는 있었으나 중복·근거 신뢰도에 대한 사용 조건 부족 | description/reference_context만 허용, 중복 제거, 근거 신뢰도 0 제외 |
| 특성 사용 | user_stated/supported/inferred 상태만 확인하여 신뢰도 0·빈 근거도 프롬프트 사용 가능 | 허용 상태, 양수 신뢰도, 비어 있지 않은 특성·허용 출처 근거를 모두 요구 |
| 충돌 상태 | 인용이 없으면 conflicting도 unknown으로 변경 | conflicting 보존, 사용 제외, 별도 안내 |
| 제외 이유 | 일반 경고만 제공 | 특성 JSON에 exclusion_reason 저장: 인용 누락, 모델 불확실, 충돌, 신뢰도 0, 참고자료 변경. 화면 경고에 제외 개수·충돌·자료 변경 안내 |
| 자료 연결 해제 | 연결만 제거하고 과거 자료 기반 특성은 남음 | 참조가 바뀌면 해당 특성을 unknown·신뢰도 0으로 무효화. 설명에서 직접 지정한 특성은 보존 |

기존에도 unknown/conflicting은 프롬프트의 특성 목록에서 제외되고 있었음. 이번에는 신뢰도·근거 조건과 자료 변경 시점까지 보완한 것.

## 변경 파일

- `build\payload\backend\app\models\persona.py`: 공통 사용 조건, 제외 이유, 경고, 참조 특성 무효화
- `build\payload\backend\app\services\persona_service.py`: 원문 인용 검증
- `build\payload\backend\app\repositories\agent_repository.py`: 메모리·PostgreSQL의 연결 해제/교체 처리
- `build\payload\llm-service\app\prompts.py`: 모델 프롬프트 사용 조건
- `build\build.ps1`: 배포 식별자 갱신

## 검증

- 신규 단위 검사 14개 통과: 인용 형태, 부정 표현, 짧은 근거, 임의 출처, 충돌, 불확실, 신뢰도, 연결 변경, 소유자 범위, 프롬프트 포함/제외.
- 기존 페르소나 회귀 검사 3개 통과.
- 실제 PostgreSQL에서 연결 해제·교체, 소유자 격리, 제외 이유 저장, 직접 지정 관점 보존 확인.
- DB 검사는 독립 임시 행과 SAVEPOINT를 사용했고 최종 외부 트랜잭션 ROLLBACK 후 테스트 계정·문서가 남지 않음을 확인.
- 현재 앱 Backend·LLM에 적용 완료. 기존 사용자 문서와 페르소나를 일괄 삭제하거나 재생성하지 않음.

## 한계와 사용

인용이 원문에 존재한다는 검증은 특성의 의미가 그 인용으로 논리적으로 입증된다는 뜻은 아님. 이 로직은 의미적 함의·실제 자격·경력의 진위까지 인증하지 않음. 특히 일부 단어만 인용하면 앞뒤 맥락이 충분하지 않을 수 있음. 추론 특성은 프롬프트에 '추론된 관점'으로 표시하며, 모델이 unknown/conflicting으로 만든 특성을 인용 일치만으로 활성화하지 않음.

기존 근거는 개별 document_id가 아닌 합쳐진 reference_context를 가리키므로 참고자료 일부만 바뀌어도 참고자료 기반 특성을 보수적으로 무효화함. 남은 자료로 재확인하려면 질문자 '수정'에서 저장해 다시 분석하면 됨. 향후 문서 ID·구간·인용 위치를 별도 저장하면 해당 자료의 특성만 선택적으로 재검증할 수 있음.

과거에 잘못된 인용으로 제거된 근거 원문은 현재 DB에 남아 있지 않을 수 있음. 이번 수정이 삭제된 근거를 자동 복원하지는 않으며, 질문자 수정·재분석 시 새 규칙이 적용됨. 이미 저장된 연습 세션의 경고는 당시 기록으로 유지.

백업: `C:\Users\임준혁\Downloads\최종\EXE폴더\backup-before-trait-validation-20260920-221624`

실행 파일: `C:\Users\임준혁\Downloads\최종\EXE폴더\Metaverse-Setup.exe`
